這些檔案是為了讓勝義工讀生可以不必花太多時間在整理下載量上，
主要分成三個步驟：

1. 從Google Developer後台下載每一支APP的資料，預設是載到電腦user的下載資料夾內。
	a. 程式會先檢查下載資料夾內有沒有舊的檔案，有的話就會刪掉。
	b. 開始讀取預設的url，修改裡面的起始日期和結束日期。
	c. 以模擬打開瀏覽器並且輸入網址的方式下載檔案。
	#1. 預設都會在每個禮拜五執行，下載資料至當週的星期三。
	#2. 執行的電腦可能要先登入Google Developer的後台
2. 讀取下載資料夾內每個剛下載下來的APP資料，之後將資料一筆筆輸入到Mysql裡面。
	a. 預先宣告好所有app的名稱和下載下來的檔案名稱。
	b. 開始遍歷所有檔案名稱、讀取檔案。
	c. 資料存到一個陣列，反過來從最後一筆開始輸入，是為了日後計算總下載量方便。
	#1. 要利用到MysqlTest.java檔，宣告Mysql class使用先寫好的變數和函數。
3. 透過已經完成的web將Mysql資料顯示出來。
	